<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- ON state -->
    <item android:color="@color/dividerColor" android:state_checked="true"/>
    <!-- OFF state -->
    <item android:color="@color/cardBgColor"/>
</selector>
